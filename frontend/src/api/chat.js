// Chat Group is WebSocket based.
// Base URL for websocket
export const WS_URL = 'ws://localhost:8080/api/chat_groups';

// No Axios endpoints for ChatGroup in router, it's a websocket upgrade on GET /chat_groups
